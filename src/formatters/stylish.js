import _ from 'lodash';

const flags = {
  default: '   ',
  add: ' + ',
  delete: ' - ',
};

const getStylishFormatDiff = (diff) => {
  const stylishDiff = {};

  return diff;
};

export default getStylishFormatDiff;
